<% graph.edges.forEach(function(edge){ graph.nodes.forEach(function(node){ if (node.id == edge.source) { %><%=node.label%>, <%} });  graph.nodes.forEach(function(node){ if (node.id == edge.target) { %><%=node.label%>
<%} }) }) %>
